// if there were going to be more routes this file is to conglomerate them into one array
import { default as NameMessage } from './NameMessage.js';

const routes = [...NameMessage];
export default routes;
